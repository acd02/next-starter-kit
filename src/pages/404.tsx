import { Box } from 'components/atoms/Box'

export default function Custom404() {
  return (
    <Box
      css={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        height: '100%',
      }}
    >
      missing page
    </Box>
  )
}
