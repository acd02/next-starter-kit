import { css } from '@emotion/core'

const root = css({
  color: '#757575',
  cursor: 'pointer'
})

const isActive = css({
  borderBottom: '1px solid'
})

export const styles = {
  root,
  isActive
}
